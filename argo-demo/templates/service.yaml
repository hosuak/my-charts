apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-demo.fullname" . }}
  labels:
    {{- include "argo-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-demo.selectorLabels" . | nindent 4 }}
